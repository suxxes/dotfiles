README.md
LICENSE
.git
.gitignore

# Don't sync tmux to Linux/LXC (using tmux from Macbook over SSH)
# Don't sync 1Password SSH agent config to Linux (no 1Password app)
# {{ if ne .machineType "mac" }}
.tmux.conf
.tmux/
.ssh/config
.config/1Password/
# {{ end }}
