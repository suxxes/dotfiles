README.md
LICENSE
.git

# Session scratch file, never a dotfile target.
CHECKPOINT.json

# CodexBar is personal macOS only.
# {{ if or (ne .osType "darwin") (ne .profile "personal") }}
.config/codexbar/
# {{ end }}

# claude-swap auto-switcher is work macOS only.
# {{ if or (ne .osType "darwin") (ne .profile "work") }}
Library/LaunchAgents/
# {{ end }}

# Mac-only configs (1Password agent, Zed editor, Ghostty) and the whole
# ~/Library tree: ignoring only files inside it still creates the parent
# directories on Linux.
# {{ if ne .osType "darwin" }}
.ssh/config
.config/1Password/
.config/zed/
.config/ghostty/
Library/
# {{ end }}
